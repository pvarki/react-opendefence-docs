{
  "schemaVersion": 1,
  "id": "27c0ac8f-5d90-411f-b061-4051fad9cb99",
  "slug": "platform-overview-f9bvJwMCcY",
  "collection": "architecture",
  "locale": "en",
  "title": "Platform overview",
  "breadcrumb": [
    "Platforms",
    "Kubernetes",
    "Platform overview"
  ],
  "createdAt": "2026-07-15T14:06:21.964Z",
  "updatedAt": "2026-07-15T14:29:30.562Z",
  "headings": [
    {
      "depth": 2,
      "text": "What this chapter covers",
      "id": "what-this-chapter-covers"
    },
    {
      "depth": 2,
      "text": "Namespaces and workloads",
      "id": "namespaces-and-workloads"
    },
    {
      "depth": 2,
      "text": "The phase model",
      "id": "the-phase-model"
    },
    {
      "depth": 2,
      "text": "One request, end to end",
      "id": "one-request-end-to-end"
    },
    {
      "depth": 2,
      "text": "Reading guide",
      "id": "reading-guide"
    },
    {
      "depth": 2,
      "text": "Related pages",
      "id": "related-pages"
    }
  ],
  "blocks": [
    {
      "type": "html",
      "html": "<blockquote>\n<p><strong>⚠️ Closed alpha — provisional docs.</strong> OpenDefence on Kubernetes (<code>opendefence-platform</code>) is in <strong>closed alpha</strong> and under rapid, breaking development. This page describes a moving target and will most likely be <strong>superseded by the documentation that ships with the first public release</strong> — treat everything here as provisional, and do not rely on it for a production deployment.</p>\n</blockquote>\n<p>This page is the map of the whole Kubernetes stack (<a href=\"https://github.com/pvarki/opendefence-platform\" target=\"_blank\" rel=\"noopener noreferrer\"><code>pvarki/opendefence-platform</code></a>) on one screen: which namespaces exist and what runs in them, the six-phase order everything comes up in, and how a request actually reaches a pod. It is written for developers and operators who are about to read the rest of this Architecture chapter and want to know where each detail lives.</p>\n<h2 id=\"what-this-chapter-covers\"><a href=\"#what-this-chapter-covers\">What this chapter covers</a></h2>\n<p>This chapter documents the <strong>Kubernetes platform core</strong>: the Kustomize manifests, PKI, ingress, service mesh, core services, data layer and observability of <code>opendefence-platform</code>. Product integrations (TAK, MediaMTX, Matrix, Battlelog) appear here only as a <em>pattern</em> — how any product namespace plugs into the core (see <a href=\"/en/architecture/integration-pattern-apg75p44wu\">Integration pattern</a>). The per-product deep dives live in their own books: <a href=\"/en/working-with-tak/tak-stack-kubernetes-ab0AvFHVX5\">TAK stack (Kubernetes)</a>, <a href=\"/en/mediamtx/mediamtx-stack-kubernetes-TcMZZqx9nj\">MediaMTX stack (Kubernetes)</a> and <a href=\"/en/matrix/matrix-stack-kubernetes-KULarUCYri\">Matrix stack (Kubernetes)</a>.</p>\n<p>Two things deliberately stay out of this chapter:</p>\n<ul>\n<li>The <strong>conceptual</strong> picture — what Deploy App is, the hub-and-spoke model, who talks to whom and why — is in <a href=\"/en/introduction/architecture-orientation-H9Vndl9nba\">Architecture orientation</a>. This chapter assumes you have read it and goes straight to concrete Kubernetes resources.</li>\n<li>The <strong>legacy Docker Compose stack</strong> and how it compares to this platform: see <a href=\"/en/introduction/choose-your-platform-legacy-docker-vs-kubernetes-HMH7bBbZTO\">Choose your platform</a> and the Legacy architecture chapter starting at <a href=\"/en/architecture/legacy-stack-overview-FKheql7C43\">Legacy stack overview</a>.</li>\n</ul>\n<p>Install and operations steps are also elsewhere — <a href=\"/en/operate/deploy-on-kubernetes-S5cwJYQzPu\">Deploy on Kubernetes</a> and <a href=\"/en/operate/day-2-operations-kubernetes-lDjKap3DpF\">Day-2 operations (Kubernetes)</a> in the Operate book.</p>\n<h2 id=\"namespaces-and-workloads\"><a href=\"#namespaces-and-workloads\">Namespaces and workloads</a></h2>\n<p>The platform is a set of Kustomize components (<code>k8s/&#x3C;component>/{base,overlays/{local,cloud}}</code>) that land in a fixed set of namespaces. The split is conventional: operator namespaces for cluster machinery, <code>opendefence-system</code> for the core, one <code>app-*</code> namespace per product.</p>\n<table>\n<thead>\n<tr>\n<th>Namespace</th>\n<th>Phase</th>\n<th>What runs there</th>\n</tr>\n</thead>\n<tbody>\n<tr>\n<td><code>traefik-system</code></td>\n<td>1</td>\n<td>Traefik (Helm chart 38.0.1). Entrypoint <code>web</code> :80 permanently redirects to <code>websecure</code> :443; three in-repo Yaegi plugins for callsign and client-certificate header handling. The <code>strip-identity-headers</code> and mTLS middleware chains also live in this namespace, though they are applied in phase 5. Local overlay runs Traefik with <code>hostNetwork</code> and hostPorts 80/443.</td>\n</tr>\n<tr>\n<td><code>cert-manager</code></td>\n<td>1–3</td>\n<td>cert-manager v1.19.2 (phase 1), trust-manager (phase 2), and the <code>opendefence-pki</code> CA-chain resources (phase 3).</td>\n</tr>\n<tr>\n<td><code>cnpg-system</code></td>\n<td>1</td>\n<td>CloudNativePG operator — manages every PostgreSQL cluster on the platform.</td>\n</tr>\n<tr>\n<td><code>external-secrets-system</code></td>\n<td>1</td>\n<td>External Secrets Operator — generates all platform passwords in-cluster via <code>Password</code> generators.</td>\n</tr>\n<tr>\n<td><code>linkerd</code></td>\n<td>3</td>\n<td>Linkerd control plane; its identity issuer is signed by the platform CA (<code>identity.externalCA: true</code>).</td>\n</tr>\n<tr>\n<td><code>monitoring</code></td>\n<td>4</td>\n<td>Prometheus + Alertmanager, Loki, Alloy, kube-state-metrics and Grafana instances (the operators and CRDs arrive already in phase 1).</td>\n</tr>\n<tr>\n<td><code>linkerd-viz</code></td>\n<td>4</td>\n<td>Linkerd dashboard extension, reusing the cluster Prometheus instead of running its own.</td>\n</tr>\n<tr>\n<td><code>opendefence-system</code></td>\n<td>5</td>\n<td>The core: <code>rmapi</code> :8000, <code>rmui</code> (nginx UI), <code>keycloak</code> (serves its own TLS on hostPort :9443), the <code>keycloak-init</code> realm-import Job, <code>openldap</code> :1389/:1636, CNPG clusters <code>rmapi-postgres</code> and <code>keycloak-postgres</code>, and the <code>mtls-optional</code> / <code>mtls-strict</code> TLSOptions.</td>\n</tr>\n<tr>\n<td><code>app-tak</code></td>\n<td>6</td>\n<td><code>tak-server</code> pod — TAK split into five JVM services plus an nginx UI and the <code>takrmapi</code> integration API :8003 — with CNPG <code>tak-postgres</code> (PostGIS).</td>\n</tr>\n<tr>\n<td><code>app-mtx</code></td>\n<td>6</td>\n<td><code>mtx-server</code> pod — <code>mediamtx</code> plus nginx UI and <code>mtxrmapi</code> :8005 — with CNPG <code>mtx-postgres</code>.</td>\n</tr>\n<tr>\n<td><code>app-matrix</code></td>\n<td>6</td>\n<td><code>matrix</code> pod — Synapse :8008 (federation disabled), Matrix Authentication Service, LiveKit SFU, the <code>matrix-rtc</code> JWT service, nginx UI and <code>matrixrmapi</code> :8012 — with CNPG <code>matrix-postgres</code> and <code>mas-postgres</code>.</td>\n</tr>\n<tr>\n<td><code>app-battlelog</code></td>\n<td>6</td>\n<td><code>battlelog</code> (<code>blapi</code> :3000, the one workload <em>not</em> in the mesh) with CNPG <code>battlelog-postgres</code> (PostGIS). The simplest product shape.</td>\n</tr>\n</tbody>\n</table>\n<p>The diagram below puts the same map in one picture, with the traffic and control edges that matter most: Traefik fans out to the UIs and APIs, and <code>rmapi</code> — the hub — talks to Keycloak, OpenLDAP and each product's integration API.</p>"
    },
    {
      "type": "mermaid",
      "code": "flowchart TB\n    subgraph ts[\"traefik-system\"]\n        traefik[\"Traefik :80/:443<br/>middlewares + callsign plugins\"]\n    end\n    subgraph ods[\"opendefence-system\"]\n        rmui[\"rmui<br/>nginx UI\"]\n        rmapi[\"rmapi :8000\"]\n        keycloak[\"keycloak<br/>own TLS, hostPort :9443\"]\n        openldap[\"openldap :1389/:1636\"]\n    end\n    subgraph apps[\"Product namespaces\"]\n        tak[\"app-tak<br/>tak-server pod, takrmapi :8003<br/>tak-postgres\"]\n        mtx[\"app-mtx<br/>mtx-server pod, mtxrmapi :8005<br/>mtx-postgres\"]\n        matrix[\"app-matrix<br/>matrix pod, matrixrmapi :8012<br/>matrix-postgres + mas-postgres\"]\n        bl[\"app-battlelog<br/>battlelog :3000, not meshed<br/>battlelog-postgres\"]\n    end\n    subgraph ops[\"Operator namespaces\"]\n        cm[\"cert-manager<br/>+ trust-manager + PKI\"]\n        eso[\"external-secrets-system\"]\n        cnpg[\"cnpg-system<br/>CloudNativePG\"]\n        l5d[\"linkerd + linkerd-viz\"]\n    end\n    subgraph mon[\"monitoring\"]\n        obs[\"Prometheus, Alertmanager,<br/>Loki, Alloy, Grafana\"]\n    end\n    traefik --> rmui & rmapi\n    traefik --> tak & mtx & matrix & bl\n    rmapi --> keycloak & openldap\n    rmapi --> tak & mtx & matrix"
    },
    {
      "type": "html",
      "html": "<p>Keycloak is intentionally missing an edge from Traefik: it terminates its own TLS on hostPort 9443 and is reached directly at <code>kc.&#x3C;domain>:9443</code> (see the entry-path diagram further down).</p>\n<h2 id=\"the-phase-model\"><a href=\"#the-phase-model\">The phase model</a></h2>\n<p>There is no GitOps controller — deployment is an imperative, ordered pipeline driven by the task runner (<code>task up</code> locally on kind, <code>task deploy:cloud</code> against k3s). Each phase is a Kustomize entry point under <code>k8s/main/&#x3C;env>/&#x3C;phase>/</code>, rendered with <code>kubectl kustomize --enable-helm</code> and applied with <code>kubectl apply --server-side</code>. The order exists because later phases consume CRDs, webhooks and certificates that earlier phases create. The six phases:</p>"
    },
    {
      "type": "mermaid",
      "code": "flowchart LR\n    p1[\"1 infra-phase1<br/>CNPG, ESO, cert-manager,<br/>Traefik, Prometheus and<br/>Grafana operators\"]\n    p2[\"2 infra-phase2<br/>trust-manager,<br/>callsign secret mirror<br/>(+ Lets Encrypt issuers, cloud)\"]\n    p3[\"3 infra-phase3<br/>opendefence-pki CA chain,<br/>Linkerd control plane\"]\n    p4[\"4 monitoring<br/>Prometheus, Loki, Alloy,<br/>Grafana, linkerd-viz\"]\n    p5[\"5 opendefence<br/>Traefik mTLS middlewares,<br/>rmapi, rmui, Keycloak,<br/>OpenLDAP, core databases\"]\n    p6[\"6 opendefence-apps<br/>app-tak, app-mtx,<br/>app-matrix, app-battlelog\"]\n    p1 --> p2 --> p3 --> p4 --> p5 --> p6"
    },
    {
      "type": "html",
      "html": "<ol>\n<li><code>**infra-phase1**</code> installs the operator layer: CloudNativePG, External Secrets Operator, cert-manager, Traefik (with its CRDs) and the Prometheus/Grafana operators — everything that defines CRDs other phases will instantiate.</li>\n<li><code>**infra-phase2**</code> adds trust-manager (which will distribute the CA bundle to every namespace) and the callsign secret mirror; the cloud overlay also creates the Let's Encrypt ClusterIssuers.</li>\n<li><code>**infra-phase3**</code> builds the platform PKI (self-signed root → intermediate → issuing ClusterIssuers, detailed in <a href=\"/en/architecture/pki-trust-4ImIJOGVIZ\">PKI &#x26; trust</a>) and installs Linkerd, whose identity issuer is signed by that same CA.</li>\n<li><code>**monitoring**</code> instantiates what the phase-1 operators made possible: Prometheus and Alertmanager CRs, Loki, Alloy, Grafana and linkerd-viz — see <a href=\"/en/architecture/observability-pipeline-rtVtxQILXS\">Observability pipeline</a>.</li>\n<li><code>**opendefence**</code> deploys the core itself — the mTLS middleware chains plus <code>rmapi</code>, <code>rmui</code>, Keycloak, OpenLDAP and the two core CNPG clusters.</li>\n<li><code>**opendefence-apps**</code> deploys the four product namespaces, each following the <a href=\"/en/architecture/integration-pattern-apg75p44wu\">integration pattern</a>.</li>\n</ol>\n<p>Between phases, the pipeline blocks on <code>kubectl rollout status</code> gates so a phase never runs against a half-ready predecessor:</p>\n<table>\n<thead>\n<tr>\n<th>After phase</th>\n<th>Waits for</th>\n<th>Why</th>\n</tr>\n</thead>\n<tbody>\n<tr>\n<td><code>infra-phase1</code></td>\n<td><code>cnpg-controller-manager</code>, <code>cert-manager-webhook</code>, <code>external-secrets-operator</code> (+ its webhook)</td>\n<td>admission webhooks must answer before later phases create their CRs</td>\n</tr>\n<tr>\n<td><code>infra-phase2</code></td>\n<td><code>trust-manager</code></td>\n<td>the <code>Bundle</code> machinery must exist before the PKI phase publishes <code>ca-bundle</code></td>\n</tr>\n<tr>\n<td><code>infra-phase3</code></td>\n<td><code>linkerd-proxy-injector</code>, <code>linkerd-identity</code>, <code>linkerd-destination</code></td>\n<td>every later pod gets its mesh sidecar injected <em>at creation</em> — the injector must be up first</td>\n</tr>\n<tr>\n<td><code>opendefence</code></td>\n<td><code>deployment/rmapi</code> (180 s), <code>deployment/keycloak</code> (300 s)</td>\n<td>the product apps register against both on startup</td>\n</tr>\n</tbody>\n</table>\n<p>One deliberate wrinkle crosses the phases: Traefik (phase 1) reads the callsign shared secret as a required env var, the mirror that copies it into <code>traefik-system</code> is applied in phase 2, but the <em>source</em> secret is only generated in phase 5 — the gap converges through External Secrets reconciliation (about 20 s) once the source exists. The full mechanism is in <a href=\"/en/architecture/data-secrets-P2GTaGOZFb\">Data &#x26; secrets</a>; the packaging and environment story (kind vs k3s, Zarf, releases) is in <a href=\"/en/architecture/deployment-model-PAchCIUark\">Deployment model</a>.</p>\n<h2 id=\"one-request-end-to-end\"><a href=\"#one-request-end-to-end\">One request, end to end</a></h2>\n<p>Traffic enters the cluster on three distinct paths — only the first one gets Traefik's middleware treatment:</p>"
    },
    {
      "type": "mermaid",
      "code": "flowchart LR\n    client[\"Client\"]\n    subgraph edge[\"Cluster edge\"]\n        traefik[\"Traefik websecure :443<br/>TLSOption + middleware chain\"]\n        kc[\"Keycloak hostPort :9443<br/>own TLS, client-auth=request\"]\n        raw[\"Raw TCP/UDP Services<br/>NodePort local / LoadBalancer cloud\"]\n    end\n    web[\"HTTP(S) backends: rmui, rmapi,<br/>product UIs and APIs, Synapse,<br/>MAS, matrix-rtc, Grafana\"]\n    kcpod[\"Keycloak pod\"]\n    takp[\"TAK: 8089, 8443,<br/>8090-8094, 9000-9002\"]\n    mtxp[\"MediaMTX: 1936, 8322, 8890,<br/>9888, 9889, 9996, 9997\"]\n    lkp[\"LiveKit: 7881, 7882,<br/>3478, 5349\"]\n    client --> traefik --> web\n    client --> kc --> kcpod\n    client --> raw --> takp & mtxp & lkp"
    },
    {
      "type": "html",
      "html": "<p>The second path exists because Keycloak terminates its own TLS (its image is built with <code>client-auth=request</code>); Linkerd treats 9443 as an opaque port and passes it through as raw TCP. The third path is the product protocols that are not HTTP at all — TAK CoT streaming and federation, MediaMTX media protocols, LiveKit TURN/media — exposed as NodePorts locally and as LoadBalancer services in the cloud, bypassing Traefik entirely. Details and the full FQDN/port table are in <a href=\"/en/architecture/ingress-network-edge-OGlnomxrzV\">Ingress &#x26; network edge</a>.</p>\n<p>For the main path, here is what happens to one HTTPS request, say <code>https://mtls.&#x3C;domain>/api/...</code>:</p>\n<ol>\n<li><strong>TLS terminates at Traefik</strong> on the <code>websecure</code> :443 entrypoint. The router's TLSOption decides the client-certificate policy: <code>mtls-optional</code> (verify if given) on hosts like <code>mtls.&#x3C;domain></code> and <code>tak.&#x3C;domain></code>, <code>mtls-strict</code> (require and verify) on <code>bl.</code>, <code>mtx.</code> and <code>matrix.&#x3C;domain></code>. Presented certificates are verified against the platform <code>ca-bundle</code>.</li>\n<li><strong>Identity headers are scrubbed.</strong> <code>strip-identity-headers</code> is attached as a default middleware on the entrypoint, so before any routing logic runs, every client-supplied <code>X-ClientCert-*</code>, <code>X-SSL-Client-*</code>, <code>Callsign*</code>, <code>l5d-client-id</code>, <code>X-Forwarded-Tls-Client-Cert*</code> and <code>Validity-Secret</code> header is deleted. Nothing downstream can be fed a spoofed identity.</li>\n<li><strong>The callsign is checked.</strong> On mTLS routes the <code>mtls-chain</code> middleware runs: it passes the client certificate onward, POSTs the verified certificate CN to rmapi's internal callsign-validity endpoint (authenticated with a shared secret), and stamps <code>Callsign</code>, <code>Callsign-Valid</code> and <code>Callsign-Valid-Reason</code> headers. An invalid callsign is 302-redirected to the base domain's error page (or gets 403). A <code>mtls-chain-with-legacy</code> variant additionally sets <code>X-ClientCert-DN</code>/<code>X-ClientCert-Serial</code> for backends that expect the legacy header contract. Full walkthrough in <a href=\"/en/architecture/edge-mtls-callsign-checks-lnct0AUxig\">Edge mTLS &#x26; callsign checks</a>.</li>\n<li><strong>The request enters the pod.</strong> Traefik forwards plain HTTP to the backend Service. Traefik itself is not meshed, but the destination pod is Linkerd-injected: the connection lands on the pod's sidecar proxy, which hands it to the application container as plain HTTP on localhost.</li>\n<li><strong>Inside the mesh, hops are mTLS again.</strong> Any follow-up in-cluster call — a product API calling <code>rmapi:8000</code>, rmapi calling a product's integration API — is written as plain HTTP but transparently wrapped in Linkerd mutual TLS, and callers pin the expected peer by its Linkerd identity CN. How identity is issued and verified is covered in <a href=\"/en/architecture/service-mesh-linkerd-xuI2Sn9cC9\">Service mesh (Linkerd)</a>.</li>\n</ol>\n<h2 id=\"reading-guide\"><a href=\"#reading-guide\">Reading guide</a></h2>\n<p>The rest of the chapter, in a sensible reading order:</p>\n<ul>\n<li><a href=\"/en/architecture/deployment-model-PAchCIUark\">Deployment model</a> — the Kustomize/component layout, the phase pipeline in depth, the task runner, kind vs k3s environments, Zarf packaging (online and air-gapped) and CalVer releases.</li>\n<li><a href=\"/en/architecture/pki-trust-4ImIJOGVIZ\">PKI &#x26; trust</a> — the self-signed root → intermediate CA chain, the trust-manager <code>ca-bundle</code> that reaches every namespace, Let's Encrypt for public certificates, and per-user client certificates minted through cert-manager.</li>\n<li><a href=\"/en/architecture/ingress-network-edge-OGlnomxrzV\">Ingress &#x26; network edge</a> — Traefik configuration, the hostname map (<code>&#x3C;domain></code>, <code>mtls.</code>, per-product hosts), middleware wiring, and the raw TCP/UDP exposure for TAK, MediaMTX and LiveKit.</li>\n<li><a href=\"/en/architecture/edge-mtls-callsign-checks-lnct0AUxig\">Edge mTLS &#x26; callsign checks</a> — TLSOptions, the three Yaegi plugins, the callsign validity/redirect chain, and the current revocation stance (OCSP disabled; the validity check is the interim enforcement).</li>\n<li><a href=\"/en/architecture/service-mesh-linkerd-xuI2Sn9cC9\">Service mesh (Linkerd)</a> — external-CA identity, which workloads are injected (and that <code>battlelog</code> is not), opaque ports, and identity pinning between rmapi and the product APIs.</li>\n<li><a href=\"/en/architecture/core-services-pAmGXLhao1\">Core services</a> — rmapi, rmui, Keycloak (and the <code>keycloak-init</code> realm import), OpenLDAP, and the core databases in <code>opendefence-system</code>.</li>\n<li><a href=\"/en/architecture/integration-pattern-apg75p44wu\">Integration pattern</a> — the anatomy of a product namespace: the <code>kraftwerk-init.json</code> contract in both directions, healthchecks, secrets and the per-product database.</li>\n<li><a href=\"/en/architecture/data-secrets-P2GTaGOZFb\">Data &#x26; secrets</a> — all seven CNPG clusters (each <code>instances: 1</code>, and honestly: <strong>no backups are configured</strong>), the ESO password generators, and the cross-namespace secret-mirror pattern.</li>\n<li><a href=\"/en/architecture/observability-pipeline-rtVtxQILXS\">Observability pipeline</a> — Prometheus operator wiring, Loki + Alloy log collection, Grafana, and linkerd-viz.</li>\n</ul>\n<h2 id=\"related-pages\"><a href=\"#related-pages\">Related pages</a></h2>\n<ul>\n<li><a href=\"/en/introduction/architecture-orientation-H9Vndl9nba\">Architecture orientation</a> — the conceptual model this chapter assumes.</li>\n<li><a href=\"/en/introduction/choose-your-platform-legacy-docker-vs-kubernetes-HMH7bBbZTO\">Choose your platform: legacy Docker vs Kubernetes</a> — how this stack relates to the legacy one.</li>\n<li><a href=\"/en/architecture/legacy-stack-overview-FKheql7C43\">Legacy stack overview</a> — the Docker Compose counterpart chapter.</li>\n<li><a href=\"/en/operate/deploy-on-kubernetes-S5cwJYQzPu\">Deploy on Kubernetes</a> and <a href=\"/en/operate/day-2-operations-kubernetes-lDjKap3DpF\">Day-2 operations (Kubernetes)</a> — the operational how-to.</li>\n<li><a href=\"/en/working-with-tak/tak-stack-kubernetes-ab0AvFHVX5\">TAK stack (Kubernetes)</a>, <a href=\"/en/mediamtx/mediamtx-stack-kubernetes-TcMZZqx9nj\">MediaMTX stack (Kubernetes)</a>, <a href=\"/en/matrix/matrix-stack-kubernetes-KULarUCYri\">Matrix stack (Kubernetes)</a> — per-product deep dives.</li>\n<li><a href=\"https://github.com/pvarki/opendefence-platform\" target=\"_blank\" rel=\"noopener noreferrer\"><code>pvarki/opendefence-platform</code></a> — the source repository.</li>\n</ul>"
    }
  ]
}
