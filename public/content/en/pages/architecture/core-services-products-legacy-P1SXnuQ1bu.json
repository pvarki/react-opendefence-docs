{
  "schemaVersion": 1,
  "id": "f6cff340-96cd-42af-bef7-ff943328318a",
  "slug": "core-services-products-legacy-P1SXnuQ1bu",
  "collection": "architecture",
  "locale": "en",
  "title": "Core services & products (legacy)",
  "breadcrumb": [
    "Platforms",
    "Docker Compose (legacy)",
    "Core services & products (legacy)"
  ],
  "createdAt": "2026-07-15T14:05:42.573Z",
  "updatedAt": "2026-07-15T14:29:28.648Z",
  "headings": [
    {
      "depth": 2,
      "text": "Core services",
      "id": "core-services"
    },
    {
      "depth": 2,
      "text": "miniwerk — the legacy provisioner",
      "id": "miniwerk--the-legacy-provisioner"
    },
    {
      "depth": 2,
      "text": "Product integration APIs",
      "id": "product-integration-apis"
    },
    {
      "depth": 2,
      "text": "The submodule model",
      "id": "the-submodule-model"
    },
    {
      "depth": 2,
      "text": "Related pages",
      "id": "related-pages"
    }
  ],
  "blocks": [
    {
      "type": "html",
      "html": "<p>This page describes the <strong>legacy Docker Compose stack</strong> (<a href=\"https://github.com/pvarki/docker-rasenmaeher-integration\" target=\"_blank\" rel=\"noopener noreferrer\"><code>pvarki/docker-rasenmaeher-integration</code></a>): the core services that make up Deploy App, the <code>miniwerk</code> provisioner that bootstraps a deployment, and how product integrations attach to the composition. It is for developers and operators who need to navigate the compose file rather than deploy it — for deployment steps see <a href=\"/en/operate/deploy-with-docker-compose-B518eLioFe\">Deploy with Docker Compose</a>. The Kubernetes counterpart of this page is <a href=\"/en/architecture/core-services-pAmGXLhao1\">Core services</a>.</p>\n<h2 id=\"core-services\"><a href=\"#core-services\">Core services</a></h2>\n<p>Everything lives in a single <a href=\"https://github.com/pvarki/docker-rasenmaeher-integration/blob/main/docker-compose.yml\" target=\"_blank\" rel=\"noopener noreferrer\"><code>docker-compose.yml</code></a>. The core of Deploy App — codename RASENMAEHER — is this set of compose services (image tags shown with the default <code>RELEASE_TAG</code> of <code>2.9.0</code>):</p>\n<table>\n<thead>\n<tr>\n<th>Compose service</th>\n<th>Image (defaults)</th>\n<th>Role</th>\n</tr>\n</thead>\n<tbody>\n<tr>\n<td><code>rmapi</code></td>\n<td><code>ghcr.io/pvarki/rmapi:1.16.0-260523-d2.9.0</code></td>\n<td>RASENMAEHER core API (FastAPI, port <code>8000</code>) — enrollment, invites, cert issuance orchestration</td>\n</tr>\n<tr>\n<td><code>rmui</code></td>\n<td><code>ghcr.io/pvarki/rmui:2.3.1-260506-default-d2.9.0</code></td>\n<td>One-shot: copies the built React UI into the <code>rmui_files</code> volume</td>\n</tr>\n<tr>\n<td><code>rmnginx</code></td>\n<td><code>ghcr.io/pvarki/nginx:1.31-d2.9.0</code></td>\n<td>Main edge proxy on <code>:443</code> — serves the UI, proxies <code>/api/</code> to <code>rmapi:8000</code></td>\n</tr>\n<tr>\n<td><code>nginx_templates</code></td>\n<td><code>ghcr.io/pvarki/nginx:templates-d2.9.0</code></td>\n<td>One-shot: delivers nginx config templates into the <code>nginx_templates</code> volume</td>\n</tr>\n<tr>\n<td><code>keycloak</code></td>\n<td><code>ghcr.io/pvarki/keycloak:24.0.5-d2.9.0</code></td>\n<td>OIDC identity provider, terminates its own TLS on <code>:9443</code> (published directly, <code>KC_HOSTNAME</code> = <code>kc.&#x3C;domain></code>)</td>\n</tr>\n<tr>\n<td><code>keycloak-init</code></td>\n<td><code>adorsys/keycloak-config-cli:6.1.6-24</code></td>\n<td>One-shot: imports the <code>RASENMAEHER</code> realm into Keycloak over internal HTTP <code>:8080</code></td>\n</tr>\n<tr>\n<td><code>openldap</code></td>\n<td><code>ghcr.io/pvarki/openldap:1.0.0-d2.9.0</code></td>\n<td>User directory (root <code>dc=example,dc=org</code>), LDAP <code>:1389</code> internal, LDAPS <code>:1636</code> published</td>\n</tr>\n<tr>\n<td><code>postgres</code></td>\n<td><code>postgis/postgis:15-3.4</code></td>\n<td>Single shared PostgreSQL for everything (PostGIS because TAK needs it)</td>\n</tr>\n<tr>\n<td><code>autoheal</code></td>\n<td><code>willfarrell/autoheal:latest</code></td>\n<td>Watches the Docker socket and restarts containers labeled <code>autoheal=true</code> that go unhealthy</td>\n</tr>\n</tbody>\n</table>\n<p>The wiring between them looks like this:</p>"
    },
    {
      "type": "mermaid",
      "code": "flowchart TB\n  browser[\"User browser\"]\n  browser -->|\"https :443\"| rmnginx[\"rmnginx :443<br/>domain + mtls.domain\"]\n  browser -->|\"kc.domain :9443\"| keycloak[\"keycloak :9443<br/>own TLS\"]\n  rmnginx -->|\"serves built UI\"| rmuifiles[\"rmui_files volume<br/>(filled by rmui one-shot)\"]\n  rmnginx -->|\"/api\"| rmapi[\"rmapi :8000\"]\n  rmapi -->|\"admin API, kcnet\"| keycloak\n  rmapi -->|\"sign CSRs, canet\"| cfssl[\"cfssl :8888\"]\n  kcinit[\"keycloak-init<br/>(one-shot)\"] -.->|\"import realm RASENMAEHER\"| keycloak\n  keycloak -->|\"user federation :1389\"| openldap[\"openldap<br/>:1389 / LDAPS :1636\"]\n  rmapi -->|\"dbnet\"| postgres[\"postgres<br/>postgis/postgis:15-3.4\"]\n  keycloak -->|\"dbnet\"| postgres"
    },
    {
      "type": "html",
      "html": "<p>Details worth knowing:</p>\n<ul>\n<li><code>rmnginx</code> serves three hostnames from one container: <code>&#x3C;domain></code> (public login page from <code>/rmui_files</code>, <code>/api/</code> proxied to <code>rmapi:8000</code>, <code>/ca/ocsp</code> proxied to the OCSP responder), <code>mtls.&#x3C;domain></code> (<code>ssl_verify_client optional</code> — <code>/api</code> and the UI demand a verified client cert and inject <code>X-ClientCert-DN</code> / <code>X-ClientCert-Serial</code> headers for <code>rmapi</code>), and <code>synapse.&#x3C;domain></code> (proxied to <code>synapse:8008</code>).</li>\n<li><code>rmui</code> is not a running server: it exits after copying the built UI into <code>rmui_files</code>, and <code>rmnginx</code> gates on <code>service_completed_successfully</code>. Product UI bundles are delivered the same way through the <code>ui_files</code> volume, served under <code>/ui/</code>.</li>\n<li>Keycloak is <strong>not</strong> behind <code>rmnginx</code> — it publishes <code>9443</code> itself with <code>--https-client-auth=request</code> and gets its TLS certificate from the shared <code>le_certs</code> volume. It federates users from <code>openldap</code>, and <code>keycloak-init</code> (re)applies the realm config from <code>keycloak/keycloak-config/</code> with <code>LDAP_CONNECTION_URL: ldap://openldap:1389</code>.</li>\n<li><code>postgres</code> is one shared instance on the <code>dbnet</code> network; init scripts in <a href=\"https://github.com/pvarki/docker-rasenmaeher-integration/tree/main/pg_init\" target=\"_blank\" rel=\"noopener noreferrer\"><code>pg_init/</code></a> create separate roles and databases: <code>keycloak</code>, <code>raesenmaeher</code>, <code>tak</code>, <code>battlelog</code>, <code>rmmtx</code>, <code>synapse</code>.</li>\n<li><code>rmapi</code> persists its JWT signing key in the <code>rmapi_data</code> volume (<code>JWT_PRIVKEY_PATH=/data/persistent/private/rm_jwtsign.key</code>) and reads its product registry from <code>/pvarki</code> (see miniwerk below). Several healthchecks in the stack are honest stubs (<code>test: 'true'  # FIXME</code>), which is one reason <code>autoheal</code> exists.</li>\n<li>Compose networks partition traffic: <code>apinet</code>, <code>kcnet</code>, <code>canet</code>, <code>ocspnet</code>, <code>dbnet</code>, <code>intranet</code>, <code>productnet</code>, <code>taknet</code> — each service joins only the networks it needs.</li>\n</ul>\n<p>The CA side (<code>cfssl</code>, <code>ocsp</code>, <code>ocsprest</code> services) is covered on <a href=\"/en/architecture/pki-cfssl-miniwerk-legacy-eMoyrdRmtO\">PKI: CFSSL &#x26; miniwerk (legacy)</a>.</p>\n<h2 id=\"miniwerk--the-legacy-provisioner\"><a href=\"#miniwerk--the-legacy-provisioner\">miniwerk — the legacy provisioner</a></h2>\n<p><code>miniwerk</code> (<code>ghcr.io/pvarki/miniwerk:1.8.2-260524-d2.9.0</code>, submodule <a href=\"https://github.com/pvarki/python-rasenmaeher-miniwerk\" target=\"_blank\" rel=\"noopener noreferrer\"><code>python-rasenmaeher-miniwerk</code></a>) is a one-shot job that runs before everything else — nearly every service in the composition has a <code>depends_on: miniwerk: condition: service_completed_successfully</code> gate. Its README calls it a \"minimal KRAFTWERK emulation\": it stands in for a central provisioning service so a full RASENMAEHER-plus-products deployment can run on any single VM. It does three things:</p>\n<ol>\n<li><strong>Resolves the deployment's FQDN plan from</strong> <code>**MW_PRODUCTS**</code><strong>.</strong> The compose file sets <code>MW_PRODUCTS: \"tak,kc,bl,mtx,matrix,synapse\"</code>. For each product (plus the base domain) miniwerk derives <code>&#x3C;product>.&#x3C;domain></code> and an <code>mtls.&#x3C;product>.&#x3C;domain></code> twin (the <code>mtls</code> prefix comes from its <code>subdomains</code> default). This is why the README's DNS checklist demands a record per product and per <code>mtls.*</code> variant.</li>\n<li><strong>Fetches one Let's Encrypt certificate covering all of those names.</strong> It publishes port <code>80</code> for the HTTP-01 challenge, runs <code>certbot certonly</code> (<code>MW_LE_EMAIL</code>, <code>MW_LE_TEST</code>; key type from <code>MW_KEYTYPE</code>, <code>rsa</code> in the composition), and copies the result into the <code>le_certs</code> volume that <code>rmnginx</code>, <code>productsnginx</code>, <code>keycloak</code>, <code>openldap</code>, MediaMTX and TAK all mount. <code>MW_MKCERT=true</code> swaps certbot for mkcert when Let's Encrypt is unreachable, and <code>MW_EXTCERT</code> delegates certs to something external entirely.</li>\n<li><strong>Writes the KRAFTWERK manifests that introduce core and products to each other.</strong> Into <code>/pvarkishares/rasenmaeher</code> (mounted by <code>rmapi</code> at <code>/pvarki</code>) it writes <code>kraftwerk-rasenmaeher-init.json</code>, mapping every product to its API URL (<code>https://&#x3C;product>.&#x3C;domain>:4626/</code>, plus a base path where configured — Battlelog's is <code>/rmapi/</code> via <code>MW_BL__API_BASE</code>), user URL and expected certificate CN (<code>certcn: &#x3C;product>.&#x3C;domain></code>). Into each <code>/pvarkishares/&#x3C;product></code> (mounted by that product's integration API at <code>/pvarki</code>) it writes a <code>kraftwerk-init.json</code> with the RASENMAEHER base URIs and a one-time CSR JWT valid for 24 hours. miniwerk's own JWT public key (<code>kraftwerk.pub</code>) is copied into every share, so <code>rmapi</code> can verify the tokens miniwerk issued. Existing manifests are never overwritten — a deployment keeps its identity across restarts.</li>\n</ol>\n<p>Per-product ports are also miniwerk settings (<code>MW_TAK__API_PORT</code>, <code>MW_MTX__USER_PORT</code>, …), all defaulting the API side to <code>4626</code> — which is where the product proxy comes in.</p>\n<h2 id=\"product-integration-apis\"><a href=\"#product-integration-apis\">Product integration APIs</a></h2>\n<p>Each product ships an <strong>integration API container</strong> — the product side of the <a href=\"/en/develop-deploy-app/core-apis-the-rmapi-contract-RUcNFLQRXT\">RASENMAEHER API contract</a>. In the legacy stack these are:</p>\n<table>\n<thead>\n<tr>\n<th>Product</th>\n<th>Compose service</th>\n<th>Image (defaults)</th>\n<th>Container port</th>\n<th>Public route</th>\n</tr>\n</thead>\n<tbody>\n<tr>\n<td>TAK</td>\n<td><code>takrmapi</code></td>\n<td><code>ghcr.io/pvarki/takrmapi:1.13.0-260508-tak5.7-RELEASE-32-d2.9.0</code></td>\n<td><code>8003</code></td>\n<td><code>tak.&#x3C;domain>:4626</code></td>\n</tr>\n<tr>\n<td>MediaMTX</td>\n<td><code>rmmtx</code></td>\n<td><code>ghcr.io/pvarki/rmmtx:1.6.0-260513-d2.9.0</code></td>\n<td><code>8005</code></td>\n<td><code>mtx.&#x3C;domain>:4626</code></td>\n</tr>\n<tr>\n<td>Matrix</td>\n<td><code>matrixrmapi</code></td>\n<td><code>ghcr.io/pvarki/matrixrmapi:1.1.1-260512-d2.9.0</code></td>\n<td><code>8012</code></td>\n<td><code>matrix.&#x3C;domain>:4626</code></td>\n</tr>\n<tr>\n<td>Battlelog</td>\n<td><code>blapi</code></td>\n<td><code>ghcr.io/pvarki/blapi:1.1.0-d2.9.0</code></td>\n<td><code>3000</code></td>\n<td><code>bl.&#x3C;domain>:4626</code></td>\n</tr>\n</tbody>\n</table>\n<p>(A fifth, the <code>fake</code> reference product <code>rmfpapi</code> from submodule <code>fpintegration</code>, runs only in the local/dev compositions — but its <code>NGINX_FP_UPSTREAM</code> variables must still be defined on <code>productsnginx</code> for the template to render.)</p>\n<p>They all share one front door: <code>productsnginx</code> (the \"consolidated nginx\"), which publishes port <code>**4626**</code> (<code>NGINX_HTTPS_PRODUCT_PORT</code>) with <code>ssl_verify_client on</code> — a valid client certificate from the deployment's own CA is mandatory, checked against <code>/ca_public/ca_chain.pem</code> with a live OCSP check against the <code>ocsp</code> service. Routing is host-based: an nginx <code>map</code> sends <code>tak.&#x3C;domain></code> (and <code>mtls.tak.&#x3C;domain></code>) to <code>takconfig:8003</code>, <code>mtx.&#x3C;domain></code> to <code>rmmtx:8005</code>, <code>matrix.&#x3C;domain></code> to <code>matrixrmapi:8012</code>, and <code>bl.&#x3C;domain></code> to <code>blapi:3000</code>, forwarding <code>X-ClientCert-DN</code> / <code>X-ClientCert-Serial</code> to the upstream. (TAK's upstream is <code>takconfig</code> because <code>takrmapi</code> runs with <code>network_mode: \"service:takconfig\"</code> and shares that container's network namespace.) A second listener on <code>**4627**</code> (<code>NGINX_HTTPS_EPHEMERAL_PRODUCT_PORT</code>) exposes only <code>/ephemeral/api</code> to TAK <strong>without</strong> a client cert — it serves the ATAK auto-import package downloads that happen before a device has a certificate.</p>\n<p>Attaching a product therefore touches several places at once: an entry in <code>MW_PRODUCTS</code> (plus a <code>ProductSettings</code> block in miniwerk's config), DNS records for <code>&#x3C;product>.&#x3C;domain></code> and <code>mtls.&#x3C;product>.&#x3C;domain></code>, <code>NGINX_&#x3C;PRODUCT>_UPSTREAM</code> variables on <code>productsnginx</code> plus a line in the nginx template's host map, and the product's own compose service. There is no single registry — the compose file, miniwerk and the nginx templates must agree.</p>\n<p>End to end, a product attaches like this:</p>"
    },
    {
      "type": "mermaid",
      "code": "sequenceDiagram\n    participant MW as miniwerk\n    participant LE as LetsEncrypt\n    participant PROD as productAPI\n    participant RM as rmapi\n    participant NG as productsnginx\n    participant EUD as client\n\n    Note over MW: MW_PRODUCTS lists tak, kc, bl, mtx, matrix, synapse\n    MW->>LE: certbot HTTP-01 for product.domain and mtls.product.domain\n    LE-->>MW: certificate written to le_certs volume\n    MW->>PROD: kraftwerk-init.json via /pvarki volume (URLs + 24h CSR JWT)\n    MW->>RM: product entry in kraftwerk-rasenmaeher-init.json (API URL + certcn)\n    PROD->>RM: kw_product_init POSTs CSR to /api/v1/product/sign_csr with the JWT\n    RM-->>PROD: signed mTLS client cert, CN = product.domain\n    EUD->>NG: https to product.domain port 4626 with client cert\n    NG->>NG: verify cert against ca_chain.pem plus OCSP\n    NG->>PROD: proxy to container port with X-ClientCert-DN header"
    },
    {
      "type": "html",
      "html": "<p>The <code>kw_product_init</code> step is a small Go helper (<a href=\"https://github.com/pvarki/golang-kraftwerk-init-helper-cli\" target=\"_blank\" rel=\"noopener noreferrer\"><code>golang-kraftwerk-init-helper-cli</code></a>, built by the <code>kwinit</code> compose service) baked into product images: on first boot it reads <code>/pvarki/kraftwerk-init.json</code>, generates a keypair and CSR for <code>&#x3C;product>.&#x3C;domain></code>, and exchanges the one-time JWT for a signed mTLS client certificate at <code>rmapi</code>'s <code>/api/v1/product/sign_csr</code>. From then on, <code>rmapi</code> and the product authenticate each other with mutual TLS: <code>rmapi</code> pins the product's expected <code>certcn</code>, and the product calls back through <code>mtls.&#x3C;domain></code>.</p>\n<blockquote>\n<p><strong>Kubernetes contrast.</strong> On the Kubernetes platform this whole machinery is gone: there is no miniwerk, no shared nginx to edit and no port <code>4626</code>. A product attaches by shipping Kubernetes manifests — its own namespace, Deployment, IngressRoute and a <code>kraftwerk-init.json</code> ConfigMap — and registering itself in <code>rmapi</code>'s product registry ConfigMap. See <a href=\"/en/architecture/integration-pattern-apg75p44wu\">Integration pattern</a> and <a href=\"/en/build-an-integration/register-your-integration-kubernetes-wjtpPdtQ2k\">Register your integration (Kubernetes)</a>.</p>\n</blockquote>\n<h2 id=\"the-submodule-model\"><a href=\"#the-submodule-model\">The submodule model</a></h2>\n<p>The integration repo contains almost no application code. Every component lives in its own repository and is pulled in as a <strong>git submodule</strong> — <code>.gitmodules</code> pins 14 of them, including <code>api</code> (<a href=\"https://github.com/pvarki/python-rasenmaeher-api\" target=\"_blank\" rel=\"noopener noreferrer\"><code>python-rasenmaeher-api</code></a>), <code>uiv2</code> (<a href=\"https://github.com/pvarki/react-rasenmaeher-ui-v2\" target=\"_blank\" rel=\"noopener noreferrer\"><code>react-rasenmaeher-ui-v2</code></a>), <code>miniwerk</code>, <code>cfssl</code>, <code>keycloak</code>, <code>takserver</code>, <code>takintegration</code>, <code>mtxauthz</code>, <code>matrixrmapi</code>, <code>battlelog</code>, <code>synapse</code>, <code>fpintegration</code>, <code>kw_product_init</code> and <code>kc_client_init</code>. The compose file's <code>build:</code> contexts point straight into these directories, so a checkout of the integration repo at a given tag is a complete, reproducible build recipe:</p>"
    },
    {
      "type": "code",
      "html": "<pre class=\"shiki one-dark-pro\" style=\"background-color:#282c34;color:#abb2bf\" tabindex=\"0\"><code><span class=\"line\"><span style=\"color:#61AFEF\">git</span><span style=\"color:#98C379\"> clone</span><span style=\"color:#D19A66\"> --recurse-submodules</span><span style=\"color:#D19A66\"> -j8</span><span style=\"color:#98C379\"> git@github.com:pvarki/docker-rasenmaeher-integration.git</span></span>\n<span class=\"line\"><span style=\"color:#7F848E;font-style:italic\">## later, after checking out a tag or pulling:</span></span>\n<span class=\"line\"><span style=\"color:#61AFEF\">git</span><span style=\"color:#98C379\"> submodule</span><span style=\"color:#98C379\"> update</span><span style=\"color:#D19A66\"> --init</span><span style=\"color:#D19A66\"> --recursive</span></span></code></pre>",
      "lang": "bash"
    },
    {
      "type": "html",
      "html": "<p>The consequence for development: a change never lands in the composition directly. You commit and push in the component's own repository (each submodule directory is a full working repo), and then a PR against <code>docker-rasenmaeher-integration</code> <strong>bumps the submodule pointer</strong> to the new commit — that pointer bump is what actually changes which code a deployment runs. Release tags on the integration repo capture a known-good combination of all submodule SHAs, which is why the README recommends deploying from tags. The day-to-day edit-rebuild loop inside this structure is described in <a href=\"/en/develop-deploy-app/iterate-on-the-core-api-docker-compose-qhg7EpPTbA\">Iterate on the core API (Docker Compose)</a>.</p>\n<h2 id=\"related-pages\"><a href=\"#related-pages\">Related pages</a></h2>\n<ul>\n<li><a href=\"/en/architecture/legacy-stack-overview-FKheql7C43\">Legacy stack overview</a></li>\n<li><a href=\"/en/architecture/pki-cfssl-miniwerk-legacy-eMoyrdRmtO\">PKI: CFSSL &#x26; miniwerk (legacy)</a></li>\n<li><a href=\"/en/architecture/networking-subdomains-legacy-sLqEtnbI80\">Networking &#x26; subdomains (legacy)</a></li>\n<li><a href=\"/en/architecture/core-services-pAmGXLhao1\">Core services</a> — the Kubernetes counterpart of this page</li>\n<li><a href=\"/en/architecture/integration-pattern-apg75p44wu\">Integration pattern</a> — how products attach on Kubernetes</li>\n<li><a href=\"/en/operate/deploy-with-docker-compose-B518eLioFe\">Deploy with Docker Compose</a></li>\n<li><a href=\"/en/develop-deploy-app/iterate-on-the-core-api-docker-compose-qhg7EpPTbA\">Iterate on the core API (Docker Compose)</a></li>\n<li><a href=\"/en/build-an-integration/integration-overview-unDmuW0qFc\">Integration overview</a></li>\n</ul>"
    }
  ]
}
